function letsGoTeam(team) {
  return `LET'S GO ${team.toUpperCase()}!!!!!!!!`;
}

function teamSucks(team) {
  return `THE ${team.toUpperCase()} SUCK!!!!!!!!`;
}

module.exports = {
  letsGoTeam,
  teamSucks,
}
